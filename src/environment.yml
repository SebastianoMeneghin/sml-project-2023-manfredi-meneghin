name: smlX
channels: 
- defaults
- conda-forge
dependencies:
- python==3.10.13
- numpy
- pandas
- joblib
- scikit-learn==1.1.1
- seaborn
- dataframe_image
- gradio
- pip
- twofish
- pygrib
- eccodes
- pandasql
- xgboost
# You also need to install "hopsworks" through "pip install hopsworks"
# You also need to install "modal" through "pip install modal"
# You also need to install "cfgrib" through "pip install cfgrib"